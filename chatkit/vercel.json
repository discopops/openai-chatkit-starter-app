{
  "buildCommand": "npm --prefix frontend install && npm --prefix frontend run build && rm -rf public && cp -R frontend/dist public",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/chatkit", "destination": "/api/index.py" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
