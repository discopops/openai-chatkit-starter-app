# Vercel environment variables

Use for adding env vars via CLI/UI.

Key points:
- Use `vercel env add <NAME>` to set env vars per environment.
- UI: Project Settings → Environment Variables.
- Vite env vars must be set in the frontend project.

Sources:
```
https://vercel.com/docs/projects/environment-variables
https://vercel.com/docs/cli/env
```
