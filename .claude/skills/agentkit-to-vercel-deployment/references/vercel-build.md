# Vercel builds and output

Use when configuring frontend build output.

Key points:
- Build output directory must exist after build.
- Vite defaults to dist.
- Output directory is set in Project Settings or vercel.json.

Sources:
```
https://vercel.com/docs/deployments/configure-a-build
https://vercel.com/docs/frameworks/vite
```
